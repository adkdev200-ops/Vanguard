If you discovered something that would make a future execution better, output exactly one memory.

Format:

<memory>
...
</memory>

Rules:

• Maximum 25 words.
• Write in past tense.
• Record only reusable knowledge.
• Mention what happened and what solved it.
• Do not include user-specific information.
• Do not explain—just state the lesson.
• If nothing new was learned, output nothing.

Good examples:

<memory>
The site used Cloudflare; browser automation succeeded after waiting for the challenge to complete.
</memory>

<memory>
The page rendered content with JavaScript; using the browser instead of HTTP requests exposed the data.
</memory>

<memory>
The API enforced rate limits; exponential backoff prevented further failures.
</memory>

<memory>
The font was unavailable; falling back to DejaVu Sans resolved rendering.
</memory>

<memory>
Lazy-loaded elements required scrolling before extraction.
</memory>

<memory>
The download button appeared after network idle, not DOM load.
</memory>

<memory>
OCR misread small text; increasing image resolution improved accuracy.
</memory>

<memory>
The website blocked headless mode; using a headed browser completed the login.
</memory>